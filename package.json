{
  "name": "eojeon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.5.0",
    "esbuild": "^0.25.0",
    "vitest": "^3.0.0"
  }
}
